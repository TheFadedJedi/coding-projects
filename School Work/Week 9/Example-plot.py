# Example: Plotting x and y coordinates
import matplotlib.pyplot as plt
x = [1, 2, 3, 4, 5]
y = [3, 6, 1, 5, 2]

# Example: Plotting x and y coordinates
x = [1, 2, 3, 4, 5]
y = [3, 6, 1, 5, 2]
plt.plot(x, y) # Plotting the data points
plt.xlabel("X-axis") # Adding a label to the x-axis
plt.ylabel("Y-axis") # Adding a label to the y-axis
plt.title("Data Plot") # Adding a title to the plot
plt.show() # Displaying the plot